# AI_detector
# click the green code button and sellect download as zip file then extract the code on you pc to a folder
#then get config.js and get your api keys then save it in the folder you extracted and move to extentions on google chrom then set developer mode after that you are gonna load the package
